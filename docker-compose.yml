version: '3.8'

services:
  piratenquest:
    image: ghcr.io/asawall/piratenquest:latest
    container_name: piratenquest
    restart: unless-stopped
    ports:
      - "3030:3030"
    environment:
      - NODE_ENV=production
      - PORT=3030
